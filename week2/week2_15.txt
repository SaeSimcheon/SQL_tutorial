---------------------------------------------------------------------------------------------------------------
Any 연산자


---------------------------------------------------------------------------------------------------------------

any연산자는 값을 서브쿼리에 의해 반환된 값 집합과 비교한다.
any 연산자는 서브쿼리의 값이 어떠한 값이라도 만족하면 성립한다.


---------------------------------------------------------------------------------------------------------------

---------------------------------------------------------------------------------------------------------------
select f.title , f.length from film f where f.length >=any(
select max(f2.length) from film f2,film_category fc
where f2.film_id = fc.film_id group by fc.category_id);


이 구문에서 만약 any 연산자가 없으면 오류 발생함

multi row가 발생한 경우에 단순히 >= 등의 비교 연산자를 사용한 비교는 불가능

---------------------------------------------------------------------------------------------------------------

=any는 in 과 동일하다
---------------------------------------------------------------------------------------------------------------

---------------------------------------------------------------------------------------------------------------
