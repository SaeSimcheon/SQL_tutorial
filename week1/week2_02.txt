------------------------------------------------------------------------

inner join

특정 컬럼에 매칭 되는 

select ~ from t1 inner join t2 on t1. = t2.

key에 의해 상호 매칭되는 경우를 추출


정규화된 테이블들을 가지고 새로운 정보를 만들기 위해서

select 선택 정보를 선택

