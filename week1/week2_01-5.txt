조회는 SQLD에서 아주 중요한 능력


Select 문

Select C1,C2,.. from Table_name

customer table

view diagram을 통해서 데이터의 구성을 Graph 형태로 보여줌

~ 엔터티 관계도라는 것을 그려줌

ctrl + enter -> 구문 실행


------------------------------------------------------------------------
ctrl + shift + e -> 실행 계획이 나옴

* 주석이랑 함께 위 커맨드를 실행하면 출력이 안 돼서 따로 실행함

seq scan이라는 node type이 나오고 실행 대상등의 정보가 나옴

순차적으로 다 읽었다는 말이라고 함

걸린 시간

------------------------------------------------------------------------

지정한 column 가져오기

필요한 column만 가져오려면 column명 지칭해서 ','을 통해 열거해주면 됨


select문의 실행순서 유의할 것

/* 실행 순서 : 
 *  from - 대상
 * select -대상 
 * 순서라고 함*/


------------------------------------------------------------------------
Alias를 통해서 짧은 이름을 지칭. 가독성 높은 스크립트를 완성할 수 있음.

select A.first_name ,A.last_name ,A.email from customer A ;

------------------------------------------------------------------------

성능에도 영향을 끼치기도 함

Alias -> 코드의 가독성 -> SQL 성능

DBMS -> 옵티마이저 -> 최적화기 -> SQL -> 가장빠르게, 저비용, 실행

